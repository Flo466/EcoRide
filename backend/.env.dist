# .env.dist (VERSIONNÉ SUR GIT)
APP_ENV=dev
APP_SECRET=changeme_this_is_a_placeholder

# DATABASE_URL (pour Doctrine, avec des placeholders)
# Les valeurs réelles seront lues depuis l'environnement ou ton .env local non versionné
DATABASE_URL="mysql://your_db_user:your_db_password@ecoride_db:3306/your_db_name?serverVersion=10.6&charset=utf8mb4"

CORS_ALLOW_ORIGIN='^https?://(localhost|127\.0\.0\.1)(:[0-9]+)?$'